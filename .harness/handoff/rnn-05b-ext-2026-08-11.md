# HANDOFF — RNN-05B-EXT: Historical-state recoverability outside the ceiling (direct H3 test) — 2026-08-11

**Result: `H3_TESTABILITY = BLOCKED_BY_UNSTABLE_BASE`. `QWEN_GDN_TRANSPLANT_GATE = DEFER`.** No stable
graded-forgetting regime exists for the delta/gated-delta substrate under interference pressure: GDN/DN
forgetting is a sharp, seed-sensitive phase transition (retain-or-collapse), so the regime H3 needs — a stable
backbone that has lost *some* old associations — could not be reached across the pre-registered seeds. The
frozen H3 machinery ran and is mechanically sound, but its recovery/harm numbers are **NOT load-bearing** under
the blocked/unstable base. No Qwen weights, no llama.cpp/serving/deploy, no TPTT, no RNN-05C, no FLA, **not
pushed**.

## Git
- Branch **master**. **Start HEAD `aad66fc`** (RNN-05B accepted) → **End HEAD `d8b91d9`**.
- Commits (small, coherent): `83ac6c5` test — memory-bound challenge + pre-registration (before any run);
  `9862e1b` results — original grid BLOCKED (immutable); `e7abcbd` experiment — amendment-1 finer grid
  (pre-committed before running); `d8b91d9` results — amend1 BLOCKED_BY_UNSTABLE_BASE.
- Tracked & staged trees CLEAN. Untracked-by-policy: `.harness/`, `git_evidence*.txt`, RNN-08 adapter dirs,
  `runs/rnn/RNN-05B-EXT/*/stdout.log`. Checkpoints stay non-Git. **Not pushed.**

## Pre-registration & calibration-rule identity (single source of truth)
`ExtConfig` → `PRE_REGISTRATION.md` == `preregistration.json` == executed constants.
`CALIBRATION_RULE_IDENTITY = PASS` (band `[0.40,0.80]`, per-seed `[0.20,0.90]`, parsed back from the human rule
string == executed constants). Memory-bound MQAR generator self-qualified: `MB_GENERATOR_SELFTEST = PASS`
(length-exact, kv-survives, **no answer leak**, no lexical shortcut, **process-stable** across a fresh
subprocess, mean write→query distance ~450 tokens at L512). Architecture = the RNN-05B-qualified LA/DN/GDN
family **unchanged** (d_model 128, d_k=d_v 64, conv_k 4, seg 64); reader = the existing `w_u` grm connector
only. num_pairs=12 (far below the RNN-05B capacity cliff ~40 — **temporal pressure, not capacity overload**).

## Substrate sanity (this family instance) — all PASS
LA/DN/GDN: `REFERENCE_PARITY=PASS` (scan↔chunked maxabs ≤1.5e-6; incremental-decode 0.0),
`FULL_MODULE_LIFECYCLE=NUMERICALLY_EQUIVALENT` (complete `{S, conv}` checkpoint = 18688 B/req),
`REQUEST_ISOLATION=PASS`. (Parity scope remains LOCAL_PORTS_ONLY; upstream-executable parity NOT_QUALIFIED —
no FLA installed, and no cheap in-stack Qwen recurrence was available to probe.)

## BASE-only calibration (the load-bearing gate)
Original pre-registered grid (9 conditions, GDN seed 42, 2500 steps each) — **bimodal cliff straddling the
band → BLOCKED (no fallback):**

| seq_len | d=0.15 | d=0.35 | d=0.55 |
|---|---|---|---|
| 512  | 0.974 | 0.968 | 0.075 |
| 768  | 0.974 | 0.860 | 0.089 |
| 1024 | 0.919 | 0.073 | 0.031 |

Amendment 1 (pre-committed `e7abcbd`): a single bounded, **BASE-only** finer distractor sweep bracketing each
transition. Scan (cheap-first): L512 f42=0.928, f46=0.957, **f50=0.787 → in band → SELECTED**. But the
pre-registered **3-seed stability confirm** on the selected condition (L512, d=0.50) failed:

| substrate | seed 42 | seed 43 | seed 44 | reading |
|---|---|---|---|---|
| **GDN** | 0.787 | 0.101 | 0.090 | 1 retains, 2 collapse |
| **DN**  | 0.329 | 0.959 | 0.991 | mirror instability |
| **LA**  | 0.998 | — | — | stable ceiling (additive; never collapses) |

GDN mean 0.326, not all seeds ∈ [0.20,0.90] → **`H3_TESTABILITY = BLOCKED_BY_UNSTABLE_BASE`**. The
interference lever is a sharp, seed-sensitive tipping point — there is no stable intermediate regime — so per
the pre-commitment (single bounded refinement; do not search indefinitely) the experiment **STOPS** here.

## Frozen-backbone comparison (RAN; NOT load-bearing under the block — descriptive only)
All 7 backbones: `BACKBONE_WEIGHT_MUTATION = 0` (frozen validity PASS). Trained readers saved durably + SHA-256
(below). BASE / param-free-MC / trained-reader (holdout) and recovery/harm:

| substrate·seed | base | paramfree | reader | net | RECOVERY | HARM |
|---|---|---|---|---|---|---|
| la·42 | 0.998 | 0.997 | 0.997 | −0.001 | 0.111 | 0.001 |
| dn·42 | 0.329 | 0.363 | 0.364 | +0.035 | 0.123 | 0.145 |
| dn·43 | 0.959 | 0.967 | 0.964 | +0.005 | 0.216 | 0.004 |
| dn·44 | 0.991 | 0.992 | 0.992 | +0.001 | 0.216 | 0.001 |
| gdn·42 | 0.787 | 0.787 | 0.790 | +0.003 | **0.163** | **0.040** |
| gdn·43 | 0.101 | 0.116 | 0.114 | +0.013 | 0.045 | 0.270 |
| gdn·44 | 0.090 | 0.093 | 0.096 | +0.006 | 0.043 | 0.368 |

Descriptive reading (unqualified, single-seed regimes): where a delta-family backbone happened to sit in a
partial-recall regime, the trained reader gave a small positive net with recovery > harm on the **one in-band
GDN seed** (0.163 vs 0.040). Where backbones collapsed (GDN 43/44), MC could not recover (recovery ~0.04) and
actively **harmed** (0.27–0.37) — a collapsed final state has no useful history to read. LA (additive ceiling)
shows no historical benefit — control holds.

## Snapshot attribution + ablation (descriptive; on the in-band GDN seed)
- **Ablation** (drop one cached snapshot, recovery on holdout): full 0.163 → drop **proximal/early** 0.132 →
  drop **irrelevant/late** 0.170 → `HISTORICAL_SNAPSHOT_CAUSAL_SIGNAL = SUPPORTED` (dropping the early snapshot
  disproportionately cut recovery). DN ablation INCONCLUSIVE.
- **Attribution** (`DESCRIPTIVE`): of 142 recovered targets, only 9.2% had the reader's **argmax** gate on a
  temporally-plausible (proximal) snapshot; mean gate on proximal 0.065 vs mean gate online 0.262. So the
  gate's *max* is usually not the early snapshot even though *removing* the early snapshot hurts — a mild
  tension that is exactly why attribution is labelled DESCRIPTIVE, not causal, and why nothing here is
  load-bearing under the block.

## Pure cache-count curve (fixed weights/positions; retain most-recent K) — `QUALIFIED`
mc_acc / recovery / harm at K=1,2,4,8 (8 checkpoints, seg 64):
- **LA**: acc ~0.997 flat; recovery ~0 (additive redundancy — control holds).
- **DN** (mixed seeds): acc 0.343→0.364; recovery 0.042→0.123 **but** harm 0.044→0.145 (net wash).
- **GDN** (in-band seed dominant): acc ~0.79 flat; recovery **0.054→0.163** rising with K at low harm
  0.010→0.040 — the H3-consistent direction, **descriptive only** (unstable base).

## Secondary 2×2 — SKIPPED
Gated on `H3_TESTABILITY == QUALIFIED`; base was BLOCKED, so the 2×2 was not run (packet §17: don't let it
consume the packet when frozen H3 is blocked).

## Cost / memory (direct-measured, L512)
Live recurrent state = 18688 B/req ({S 16384 + conv 2304}); 8 historical checkpoints. Total MC forward:
LA 3.4 ms, DN 25.2 ms, GDN 25.3 ms (delta recurrence ~7× LA — forward-substitution + inter-chunk, pure
PyTorch). wall_min (amend1) = 19.5; total GPU well under the 1-hr target.

## Failures / blocked / negative evidence (explicit)
- Original grid: BLOCKED (granularity — band between coarse tiers).
- Amend1: BLOCKED_BY_UNSTABLE_BASE (seed-sensitive phase transition at the edge).
- A `nohup`-detached WSL launch was killed on shell exit (WSL tears down detached jobs); re-launched in the
  foreground of a tracked background task. No experiment-invalidating errors.

## Reproduction (CPU selftests need no GPU)
```
V=/home/augus/tptt-venv/bin/python ; PP=/mnt/c/projects/local-model-lifecycle/ops
PYTHONPATH=$PP CUDA_VISIBLE_DEVICES= $V $PP/rnn_05b_ext.py --preregister runs/rnn/RNN-05B-EXT
PYTHONPATH=$PP CUDA_VISIBLE_DEVICES= $V $PP/rnn_05b_ext.py --selftest runs/rnn/RNN-05B-EXT/mb_generator_selftest.json
# original grid (GPU): --run --outdir runs/rnn/RNN-05B-EXT --artifacts .harness/artifacts_ext
# amend1 (GPU):        --run --grid-json runs/rnn/RNN-05B-EXT/amend1_grid.json --outdir runs/rnn/RNN-05B-EXT/amend1 --artifacts .harness/artifacts_ext
```

## Durable artifacts (non-Git) — SHA-256 (first 24 hex) / bytes
```
0d21045074182b8788ccae56  377257  rnn05bext_dn_seed42_frozen_reader.pt
ed1fe696c37f2a552dd8af50  377257  rnn05bext_dn_seed43_frozen_reader.pt
b065ac276fc7f74497527472  377257  rnn05bext_dn_seed44_frozen_reader.pt
84b3c71d562104a419186a34  377274  rnn05bext_gdn_seed42_frozen_reader.pt
e97fe57c5dc777d5987188cc  377274  rnn05bext_gdn_seed43_frozen_reader.pt
709478da81575784d2fac195  377274  rnn05bext_gdn_seed44_frozen_reader.pt
89f1aa25daf0a06cdaff0e70  377257  rnn05bext_la_seed42_frozen_reader.pt
```

## Verdicts
- `H3_TESTABILITY = BLOCKED_BY_UNSTABLE_BASE`
- `GDN_HISTORICAL_RECOVERY = NOT_APPLICABLE (blocked)` — the raw label emitted over 3 mixed seeds was NO_EFFECT
  but is non-load-bearing; DN emitted WEAK_DIRECTIONAL, likewise descriptive.
- `HISTORICAL_SNAPSHOT_CAUSAL_SIGNAL = SUPPORTED` (descriptive, in-band GDN seed) · `LA_FALSIFICATION =
  CONTROL_HELD`
- `QWEN_GDN_TRANSPLANT_GATE = DEFER` (no defensible positive mechanism in a *qualified* regime; base untestable)

## Exactly one recommended next packet
**RNN-05B-EXT2 — a STABLE graded-forgetting lever (new pre-registration).** The interference lever produces a
catastrophic, seed-sensitive phase transition, not graded forgetting, so H3 is untestable here. Replace it with
a lever that yields a GDN base stable in [0.40,0.80] across ≥3 seeds: e.g. (a) associations with *staggered
per-item decay pressure* so exactly a controllable fraction of the oldest items fall out of the final state
while the backbone stays stable; and/or (b) stabilize the retain solution (lower lr / mild regularization /
seed-screening to only stable seeds) then increase the retention gap *continuously*. Keep everything else
(architecture, reader, 2×2, ≥3 seeds for DN and GDN, recovery/harm + ablation + LA control). Do NOT touch
Qwen/serving/llama.cpp/deploy; no RNN-05C.
