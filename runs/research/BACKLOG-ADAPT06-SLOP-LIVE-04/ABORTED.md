# Execution aborted fail-closed

The physical request matrix completed and the original unit restarted healthy, but restoration validation compared the full systemd `ExecStart` serialization, including volatile `start_time` and `pid`. It therefore rejected a correct restart. No scores or claims are accepted from the preserved R4 observations. A successor may change only restoration comparison to the immutable executable path and `argv[]` portion.
