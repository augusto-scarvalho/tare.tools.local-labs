# Execution aborted fail-closed

The active tokenizer returned some malformed-UTF8 token pieces as integer byte arrays, while the frozen R2 parser required every piece to be a string. Execution stopped during the real teacher panel before baseline generation or gate scoring. No claim is permitted. A successor may only normalize byte-array pieces with deterministic UTF-8 replacement decoding.
