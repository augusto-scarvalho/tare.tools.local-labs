# BACKLOG-MTP-PERSISTENCE-01 result

`MTP_PERSISTENCE_HYPOTHESIS_REJECTED` pending independent review.

Observed `20` failures in 20 unprimed fresh MTP processes and `20` failures in 20 primed processes. The four no-spec controls had `0/4` successes. Failed gates: `original_failure, controls, fixed_repeats`.
