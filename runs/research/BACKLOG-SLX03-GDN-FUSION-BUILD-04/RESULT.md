# BACKLOG-SLX03-GDN-FUSION-BUILD-04 result

`SLX03_GDN_FUSION_BUILD_CALLABLE_R4` pending independent review.

Retained hashes match `True`; linked objects `True`; ELF symbols `True`; dispatcher call edge `True`; SM86 cubins `140` only `True`; dereferenced CUDA library `63364248` bytes; self-linkage `True`; version exit `0`; services unchanged `True`. Failed gates: `none`. No runtime branch or performance claim is made.
