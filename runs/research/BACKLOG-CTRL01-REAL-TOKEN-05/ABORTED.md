# Aborted before receipt

All independent JSON/token/latency scores matched the immutable source exactly. The only disagreement was the runtime-binding bit: the generic repository search counted this recovery script's frozen source-path string `ast_grammar_sidecar.py` as a production integration. The package stopped before emitting evidence.

After `BLOCKED`, the shared binding search was restricted to `src/` and `ops/`, which are the production/runtime trees. A clean successor must freeze the corrected implementation and repeat the deterministic rescore.
