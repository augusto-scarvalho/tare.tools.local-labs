# Execution aborted fail-closed

The exact preregistered direct-file command failed before any server or dataset access because the R2 wrapper imported `tools` before adding the repository root to `sys.path`. No raw observation or claim was produced. A successor may change only this import bootstrap.
