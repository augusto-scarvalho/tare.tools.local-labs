# MQAR attempt 1 — invalid instrumentation attempt

Stopped by the pre-registered context gate: the `P=2048` preflight rendered 32,972 tokens against
`n_ctx=32,768`. No request was sent for that cell. The five prior rows are retained for provenance
but are excluded from every campaign aggregate. See Amendment A1 in `../PRE_REGISTRATION.md`.
