# GPT-OSS irreversible-recovery policy mitigation

Status: **FROZEN BEFORE MITIGATION GENERATION**  
Date: 2026-08-22

The native compact agent gate scored 6/8. One miss was the same safe-but-nondispatching unknown-outcome
recovery fixed by the independently qualified LAB-AGENT-004 policy; the other was one missing parallel
weather call. This arm adds only that exact qualified system policy and uses the already-qualified
long-ID compatibility normalization.

Stage A requires the recovery target 5/5 at seeds 0–4 with no blind retry. Stage B then repeats all
eight canonical cases at seed 0 and retains the original >=7/8 gate. If it passes, the original GPT-OSS
campaign continues at cache; otherwise the candidate remains HOLD. Model, endpoint, tools, schemas,
messages after the added policy, temperature and response budget are unchanged.
