{
  "id": "BACKLOG-GATEWAY-ROUTE-STRESS-02",
  "priority": 1,
  "title": "Rebind retained gateway route stress with semantic success separation",
  "evidence_class": "serving_runtime",
  "packet_dir": "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-02",
  "depends_on": [],
  "source_artifacts": [
    "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-01/raw/receipt.json",
    "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-01/raw/samples.jsonl",
    "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-01/raw/switches.jsonl",
    "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-01/raw/effective_route.json",
    "runs/research/BACKLOG-GATEWAY-ROUTE-STRESS-01/raw/service_identity.json",
    "docs/research/INDEPENDENT_AUDIT_LEDGER_2026-08-27_GPT56_SOL_XHIGH.md",
    "config/qualified_model_fleet.json"
  ],
  "required_evidence": [
    "acceptance_gates",
    "artifact_hashes",
    "effective_route",
    "hardware_metrics",
    "independent_evaluation",
    "paired_baseline",
    "provenance",
    "raw_samples",
    "receipt_fingerprint",
    "recovery_state",
    "service_identity",
    "source_execution_receipt",
    "statistical_analysis"
  ],
  "acceptance_gates": [
    {"id": "source_receipt", "metric": "source_receipt_digest_verified", "operator": "eq", "threshold": true},
    {"id": "immutable_sources", "metric": "final_source_set_immutable", "operator": "eq", "threshold": true},
    {"id": "switch_coverage", "metric": "recomputed_switches", "operator": "eq", "threshold": 30},
    {"id": "request_coverage", "metric": "recomputed_requests", "operator": "eq", "threshold": 120},
    {"id": "transport_integrity", "metric": "http_transport_success_rate", "operator": "eq", "threshold": 1.0},
    {"id": "route_identity", "metric": "route_alias_match_rate", "operator": "eq", "threshold": 1.0},
    {"id": "text_content_integrity", "metric": "eligible_text_nonempty_content_rate", "operator": "eq", "threshold": 1.0},
    {"id": "cycle_repeatability", "metric": "exact_cycle_repeat_rate", "operator": "ge", "threshold": 0.95},
    {"id": "artifact_table", "metric": "verified_distinct_model_artifacts", "operator": "eq", "threshold": 6},
    {"id": "gateway_integrity", "metric": "gateway_service_restarts", "operator": "eq", "threshold": 0},
    {"id": "embedding_integrity", "metric": "embedding_boundary_successes", "operator": "eq", "threshold": 30},
    {"id": "service_recovery", "metric": "initial_model_restored", "operator": "eq", "threshold": true}
  ],
  "allowed_claim_codes": [
    "QUALIFIED_GATEWAY_ROUTE_TRANSPORT_STRESS_REBOUND_R2",
    "QUALIFIED_GATEWAY_ROUTE_TRANSPORT_STRESS_NOT_CONFIRMED_R2"
  ],
  "forbidden_claims": [
    "new inference",
    "vision task success from text-only probes",
    "semantic success for reasoning-only empty-content responses",
    "model quality superiority",
    "multi-resident serving",
    "reliability beyond the retained 30-switch campaign",
    "promotion without independent review"
  ],
  "next_action": "Recompute the retained 30-switch campaign from immutable final raws, separating HTTP transport and route identity from semantic content eligibility.",
  "supersedes": "BACKLOG-GATEWAY-ROUTE-STRESS-01"
}
